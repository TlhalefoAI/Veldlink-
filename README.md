This branch was created to fix sync failures caused by unstable power and weak network conditions
The branch allows developers to test sync recovery improvements safely without affecting the main system.
